server:
  port: ${PORT:8888}

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/VictoriaMRamirez/EspBackMSParcial
          default-label: master
          search-paths: config-data
          force-pull: true