server:
  port: ${PORT:8888}

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/VictoriaMRamirez/EspBackMSParcial
          default-label: develop
          search-paths: config-data
          force-pull: true